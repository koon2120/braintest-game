import { ref } from "vue";

const playerData = ref({
    // player level
    levelDefault:0,
    // player level for show in start page
    level3:0,
    level5:0,
    level10:0
})

export default playerData